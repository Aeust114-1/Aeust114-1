<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <title>系統設定</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../public/css/<%= theme %>.css">
</head>
<body>
    <div class="container mt-5">
        <div class="card p-4">
            <h2>系統設定</h2>
            <hr>
            <form action="/settings" method="POST">
                <div class="mb-3">
                    <label class="form-label">選擇介面風格：</label>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="theme" value="light" <%= theme === 'light' ? 'checked' : '' %>>
                        <label class="form-check-label">亮色模式 (Light)</label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="theme" value="dark" <%= theme === 'dark' ? 'checked' : '' %>>
                        <label class="form-check-label">暗黑模式 (Dark)</label>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">儲存設定</button>
                <a href="/dashboard" class="btn btn-secondary">返回儀表板</a>
            </form>
        </div>
    </div>
</body>
</html>