demo網頁已成功串接資料庫，下方有資料庫測試資料，server.js中的密碼改為自己設定的，執行網頁前請先`node server.js`，再開啟網頁
style1.css拿掉華麗的特效
style1.css擁有華麗的特效
## 前置作業
`npm install`

```
-- 切換到你的資料庫
USE global_tech_db;

-- 1. 重置並建立【人工智慧】表格
DROP TABLE IF EXISTS ai_projects;
CREATE TABLE ai_projects (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100),
    description TEXT,
    category VARCHAR(50),
    icon VARCHAR(50)
);
INSERT INTO ai_projects (title, description, category, icon) VALUES 
('機器學習', '自動化數據分析模型與預測，提升決策效率。', 'analysis', 'fa-robot'),
('神經網絡', '模擬人腦運作的深度運算架構，處理複雜運算。', 'analysis', 'fa-microchip'),
('視覺辨識', '高精度即時影像處理技術，應用於安防監控。', 'vision', 'fa-eye'),
('監控分析', '即時串流影像分析與警示系統。', 'vision', 'fa-video');

-- 2. 重置並建立【資安防護】表格
DROP TABLE IF EXISTS security_logs;
CREATE TABLE security_logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    status_text VARCHAR(50),
    status_type VARCHAR(20),
    ip_address VARCHAR(45),
    threat_level VARCHAR(20),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    category VARCHAR(20)
);
INSERT INTO security_logs (status_text, status_type, ip_address, threat_level, category) VALUES 
('攔截', 'warning', '192.168.1.105', '高', 'warning'),
('正常', 'success', '10.0.0.52', '低', 'normal'),
('正常', 'success', '172.16.0.3', '低', 'normal'),
('異常', 'warning', '192.168.1.88', '中', 'warning'),
('監測', 'success', '10.0.0.12', '低', 'normal');

-- 3. 重置並建立【雲端架構】表格
DROP TABLE IF EXISTS cloud_metrics;
CREATE TABLE cloud_metrics (
    id INT AUTO_INCREMENT PRIMARY KEY,
    metric_name VARCHAR(100),
    category VARCHAR(50),
    current_value INT,
    max_value INT DEFAULT 100,
    color_theme VARCHAR(20)
);
INSERT INTO cloud_metrics (metric_name, category, current_value, max_value, color_theme) VALUES 
('伺服器負載 (US-East)', 'server', 75, 100, 'default'),
('記憶體使用率', 'server', 45, 100, 'purple'),
('資料庫連線數 (SQL)', 'db', 920, 1000, 'green'),
('Redis 快取命中率', 'db', 88, 100, 'green');

-- 4. 重置並建立【永續科技】表格
DROP TABLE IF EXISTS eco_initiatives;
CREATE TABLE eco_initiatives (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100),
    description TEXT,
    category VARCHAR(50),
    features JSON
);
INSERT INTO eco_initiatives (title, description, category, features) VALUES 
('2025 淨零碳排目標', '我們致力於將數據中心的能源效率提升 40%，並全面採用再生能源供電，落實企業社會責任。', 'goal', '["太陽能發電板覆蓋率 80%", "智慧冷卻系統", "廢熱回收機制"]');
```